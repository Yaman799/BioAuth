"""UI-state helpers for BioAuth.

This package owns the mapping from backend runtime state to user-facing text.

Modules:
  labels   — runtime status labels (was bridge/runtime_labels.py)
  safe_text — user-safe status text filtering (was duplicated across 3 QML files)
"""
